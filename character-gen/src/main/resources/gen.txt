package com.school;
import com.character.api.CharApi;
public class $ {className}
implements CharApi {
    @Override
    public void printMe() {
        System.out.println(getClass().getSimpleName() + "${someAdd}");
    }
}